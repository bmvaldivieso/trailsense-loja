import 'package:flutter/material.dart';

class SenderosScreen extends StatelessWidget {
  const SenderosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Senderos - Próximamente'));
  }
}